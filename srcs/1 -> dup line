1 -> dup line
2 -> make temp with token char
3 -> check the quotes
4 -> split the cmd with pipe (-> |) and EOF (-> '\0')
	check the validity of the pipe 
5 -> create the commands nodes :
	1. dup the complete cmd in new_cmd->cmd
	2. dup all the args of the command in new_cmd->args :
		- if the token change go to the next arg
	3. handle the redirection
	